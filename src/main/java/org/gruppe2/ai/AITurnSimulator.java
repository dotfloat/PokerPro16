package org.gruppe2.ai;

import java.util.List;

import org.gruppe2.game.Card;
import org.gruppe2.game.RoundPlayer;

public class AITurnSimulator {
	/**
	 * Simulates a number of rounds with the current AI hand. Returns a hand strength
	 * which can be used to decide which actions to perform
	 * @param roundPlayer
	 * @param communityCards
	 * @param numberOfRounds number of rounds to simulate
     * @return
     */
	public double getHandStregth(RoundPlayer roundPlayer, List<Card> communityCards, int numberOfRounds){
		return 0;
	}
}
